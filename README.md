# GitHub Pages (may2333.github.io template)

This package keeps only **Overview**, **Code**, and **BibTeX** sections.

## Deploy
Upload all files to your GitHub Pages repo root and enable Pages from `main` + `/(root)`.

## Edit links
Update the repo/arXiv links in the **Code** section inside `index.html`.
